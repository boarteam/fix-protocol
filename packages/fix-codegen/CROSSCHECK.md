# FIX 4.4 cross-check: Markdown-generated dictionary vs QuickFIX FIX44.xml

Generated by `@boarteam/fix-codegen` (`crosscheck` command). The Markdown reference is the
primary source; QuickFIX is an independent cross-check. Each difference is pinned by an
exact signature in `crosscheck-baseline.json`; the CI drift gate fails on anything else.

## Fields excluded up front

- **201** — PutOrCall — deprecated in FIX 4.4 (use CFICode); retained by QuickFIX in Instrument
- **315** — UnderlyingPutOrCall — deprecated in FIX 4.4; retained by QuickFIX in UnderlyingInstrument

## Differences by category

- `component-only-in-md`: 19
- `component-only-in-xml`: 98
- `field-enum`: 69
- `field-name`: 7
- `field-only-in-md`: 2
- `field-type`: 3
- `message-category`: 1
- `message-name`: 84
- `message-reqd`: 2300
- `message-structure`: 10

## Explained by cluster

### category-difference (1)

The two sources classify the message admin/app differently.

### component-factoring (117)

The sources factor the same fields into differently-named components; because messages are compared with components expanded, this never changes the decoded field sequence.

### enum-coverage (69)

The two sources enumerate different fields / value sets (e.g. B2BITS ships the ISO currency & country tables; QuickFIX enumerates some fields B2BITS leaves open).

### field-catalog (2)

A field one source defines and the other omits — each curates its own field catalog.

### field-name-alias (7)

The two sources use different names for the same tag (e.g. B2BITS `IOIid` vs QuickFIX `IOIID`, `LinesOfText` vs `NoLinesOfText`). The shipped dictionary uses the B2BITS names.

### field-type-granularity (3)

The two sources pick different datatypes with the same base primitive (e.g. B2BITS `int` vs QuickFIX `SeqNum`/`char`); value coercion is driven by the base primitive.

### naming-convention (84)

Cosmetic message display-name difference (`New Order - Single` vs `NewOrderSingle`); the MsgType and structure are identical.

### nested-group-reconstruction (10)

The flattened single-`»` Markdown source under-specifies these messages' nested repeating-group bodies; the shipped dictionary reconstructs them as best it can (see the dictionary's own coverageGaps) and differs from QuickFIX's fully-specified bodies.

### optionality-disagreement (144)

The sources disagree on a member's required-ness (`Y`/`N`) — mostly B2BITS marking Instrument.Symbol / UnderlyingInstrument.UnderlyingSymbol required where QuickFIX does not.

### quickfix-no-conditional (2156)

B2BITS marks the member conditionally-required (`C`); QuickFIX has no conditional concept and models it optional (`N`).

**Unexpected (drift): 0** · 
**Resolved (stale baseline entries): 0**
